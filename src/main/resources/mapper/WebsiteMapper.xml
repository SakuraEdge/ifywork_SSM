<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ifywork.ifywork_ssm.mapper.WebsiteMapper">
    <!-- 添加网站信息 -->
    <insert id="addWebsite" parameterType="com.ifywork.ifywork_ssm.bean.Website">
        insert into website
            (name, url, age, country)
        values (#{name}, #{url}, #{age}, #{country})
    </insert>
    <!-- 查询所有网站信息 -->
    <select id="selectAllWebsite"
            resultType="com.ifywork.ifywork_ssm.bean.Website">
        select *
        from website
    </select>
    <!-- 删除网站信息 -->
    <delete id="deleteWebsite" parameterType="java.lang.Integer">
        delete from #{id}
    </delete>
</mapper>